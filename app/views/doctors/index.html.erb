<h1> All Doctors </h1>

 <% @doctors.each do |doctor| %>
    <li>
        <%= link_to doctor.name, dcotor_path(doctor) %>
        <%= dcotor.location %>
        specialty: <%= doctor.specialty %> -
    </li>
<% end %>